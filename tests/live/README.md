# Live staging tests

Live tests require approved staging credentials and run only after the SIG machine identity exists. They must verify exact area, geometry fingerprint, result numbers, no fallback AOI, and no secrets in captured output. Automated live checks must not mint public receipts.
