# Load and recovery tests

Increment 7 adds two concurrent GIS jobs, 50-user result-read traffic, queue-depth measurement, and a full staging restore rehearsal. Production must not move to two workers until the 8-CPU load test passes.
