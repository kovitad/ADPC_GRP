# Database migrations

Alembic migrations are forward-only and run once as a separate release step. Increment 1 will add the architecture-defined access, reference-data, assessment, and control tables. Do not create a migration until the corresponding model and acceptance tests are reviewed.
